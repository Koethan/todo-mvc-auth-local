<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body class="to-change">
    <h2 id="welcome-statement">Welcome <%= user.userName %></h2>
    <div class="container">
        
        <div id="info" class="user-info">
            <button id="color-switcher" class="button-55" role="button">Toggle light/dark modes</button>
            <p id="quote"></p>
            <h2>Todays date is: <%= new Date().toDateString() %></h2>
        </div>

        <div id="todo" class="user-info">
            <h1>Todos</h1>
            <ul>
            <% todos.forEach( el => { %>
                    <li class='todoItem' data-id='<%=el._id%>'><input type="checkbox" class="myCheck">
                        <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                        <span class='del'> Delete </span>
                    </li>
            <% }) %>    
            </ul>

            <h2><%= user.userName %> has <%= left %> things left to do.</h2>

            <form action="/todos/createTodo" method='POST'>
                <input type="text" placeholder="Enter Todo Item" name='todoItem'>
                <input type="submit">
            </form>

            <a href="/logout">Logout</a>
        </div>
    </div>

    <script src="js/main.js"></script>
</body>
</html>